result*
.direnv/
# Matches generated dependency directories and the Pi extension type symlink;
# trailing-slash patterns would not cover symlinks with these names.
node_modules
.pi-types
__pycache__/
*.py[cod]
