# Archived Skills

Archived skill snapshots kept for reference while simplifying the active skill
set.

This directory is intentionally outside `skills/`, because `registries.nix`
auto-registers every directory under `skills/` for deployment to agent
frontends.
