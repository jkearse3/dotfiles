# Markdown

Wrap agent-authored `.md` files at 100 columns. Fenced code blocks and tables follow the formatting
of their contents.

Prefer rewording before exceeding the limit. Do not split URLs, file paths, or inline code spans.
