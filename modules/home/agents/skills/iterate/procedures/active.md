# Active Work

Use only for `Status: active`.

Before dispatching implement or verify, read:

- `references/active-dispatch.md`

1. Validate `Next` is `implement` or `verify`.
2. Snapshot the state file.
3. Dispatch implement or verify as a subagent according to `Next`.
