{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "command -v treefmt >/dev/null 2>&1 && command -v jq >/dev/null 2>&1 && treefmt --quiet \"$(jq -r '.tool_input.file_path')\" 2>/dev/null; exit 0"
          }
        ]
      }
    ]
  }
}
